task testJar(type: Jar, dependsOn: testClasses) {
    baseName = "${project.archivesBaseName}-tests"

    from sourceSets.test.output
    include '**/MovieMock.class'
}

configurations {
    testJ {
        extendsFrom(testRuntime)
    }
}

artifacts {
    testJ testJar
}